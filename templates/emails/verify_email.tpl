Dear {$username},

You recently registered to <a href="{$baseurl}">{$site_name}</a> using this email address. To complete your registration, follow the link below:
<a href="{$baseurl}/confirm?id={$uid}&code={$code}">{$baseurl}/confirm?id={$uid}&code={$code}</a> (If clicking on the link doesn't work, try copying and pasting it into your browser.)

Thanks,
The {$site_name} Team

___
To control which emails you receive from {$site_name}? Go to: <a href="{$baseurl}/user/prefs">{$baseurl}/user/prefs</a>